# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyo
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Database runtime
delta.db
delta.db-shm
delta.db-wal
delta.pid

# Dati operativi locali (privacy — non pubblicare)
data/academy_progress.json
exports/

# Log
logs/

# macOS
.DS_Store
Icon

# Dataset captures (generati a runtime)
datasets/captures/
datasets/training/
datasets/training_flower/
datasets/training_fruit/
datasets/learning_by_doing/
# Dataset Dipladenia — immagini di addestramento (non in repo, troppo grande)
"datasets/DIPLADENIA FIORE/"
datasets/DIPLADENIA*/

# Immagini caricate via Telegram
input_images/

# Credenziali
data/auth.json
.venv312/
# Modello GGUF llama.cpp (troppo grande per GitHub)
models/*.gguf
# Modello Keras (troppo grande per GitHub: 21MB)
models/dipladenia/*.keras
# Solo il TFLite (2.7MB) viene committato
